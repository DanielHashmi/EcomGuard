# Optional override for physical devices or custom backend hosts.
# Web defaults to http://localhost:8000 and native Expo clients derive the host automatically.
EXPO_PUBLIC_API_URL=http://192.168.1.100:8000
